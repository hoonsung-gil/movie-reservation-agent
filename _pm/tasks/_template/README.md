# tasks/ 폴더 규칙 (삭제 금지)

- 진행 중인 작업마다 `_pm/tasks/{작업명}/` 폴더를 생성
- 메인 문서: `_pm/tasks/{작업명}/{작업명}.md` — 목적, 배경, 설계, 수정 대상 파일, 안전성 체크리스트, 미결정 사항
- 관련 분석/메모 파일은 같은 폴더에 추가
- 작업 완료 시 `_pm/done/{YYYYMMDD}_{주제}/` 로 이동
- tasks/가 비어있으면 진행 중 작업 없음을 의미
